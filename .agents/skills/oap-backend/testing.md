# Testing Rules

Run targeted tests first when behavior changes. Run broader regression tests only when justified by impact.

Choose only task-relevant categories: unit, integration, API contract, authentication, ownership, or regression.

Tests are not required for documentation-only or configuration-review-only tasks. If skipped, report exactly why.

Never claim a test passed if it was not executed, and never fabricate test results.
